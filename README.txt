15 Studios QR Decoder Fix

Upload index.html to the root of the 1-5-STUDIO- GitHub repository and replace the existing index.html.

This update keeps the 1-5 Studios scene-package QR system and adds a stronger QR fallback decoder for dense QR images. It first tries the browser BarcodeDetector, then falls back to jsQR loaded from jsDelivr.

No other site files need to change.
